# Proyecto-Integrador
Gestion de almacen comercial

Si estas leyendo esto, seguramente es porque acabas de clonar el repositorio, instala lo siguiente para que no te dé error:

npm install 
npm install -g pnpm
pnpm add bcryptjs
pnpm install cors
pnpm install dotenv
pnpm install express
pnpm add jsonwebtoken
pnpm install mysql2
pnpm install nodemon